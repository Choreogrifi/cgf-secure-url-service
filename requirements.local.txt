# requirements.txt
# Description: Lists Python dependencies for the Fintech Gateway project.
# Use pip install -r requirements.txt to install these.

# --- Core Framework ---
fastapi>=0.100.0,<0.112.0   # The main asynchronous web framework
uvicorn[standard]>=0.23.0,<0.28.0 # ASGI server (includes performance extras like uvloop, httptools)

# --- Data Validation & Settings ---
pydantic>=2.5.0,<3.0.0      # Core data validation and parsing library
pydantic-settings>=2.1.0,<3.0.0 # For loading settings from environment/files

# --- HTTP Client ---
httpx>=0.25.0,<0.28.0        # Asynchronous HTTP client (for calling switches)

# --- Google Cloud & API Dependencies ---
google-api-python-client    # For interacting with Google APIs (e.g., Gmail API)
google-auth-httplib2        # Google authentication library for HTTPX
google-auth-oauthlib        # Google authentication library for OAuth 2.0
google-cloud-pubsub         # For interacting with Google Cloud Pub/Sub

# --- Configuration & Utilities ---
python-dotenv>=1.0.0,<2.0.0  # For loading .env files during local development
PyYAML>=6.0,<7.0             # For parsing YAML configuration files (e.g., routing.yaml)

# --- Testing Dependencies (Optional but recommended) ---
pytest>=7.4.0,<9.0.0          # Testing framework
pytest-asyncio>=0.21.0,<0.24.0 # For testing async code with pytest
pytest-mock>=3.10.0,<4.0.0   # Mocking library integration (uses unittest.mock)

# --- Development Tools ---
black
flake8
pytest
ipython